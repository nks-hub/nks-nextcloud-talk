name: talk_protocol
description: Typed Nextcloud Talk wire contracts for the NKS Flutter client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  punycoder: ^0.3.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
