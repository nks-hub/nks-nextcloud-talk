Fixed: the cause behind previews that never loaded.

The app asked the server for two sizes of the same photo at once — a small one for the bubble and a large one for the opened picture. Nextcloud can answer such a pair with an error and delete the full-size preview every other size is derived from, after which the photo never loads again. The app now asks for one size at a time.
